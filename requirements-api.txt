# API runtime dependencies (pinned).
# This is the slim set that ships in the production container. It includes
# only what the inference path needs: web framework, model libraries used
# during serialization, the LLM client, persistence, and observability.

# --- Web framework ---
fastapi==0.115.4
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.1

# --- Inference (the same model libraries the artifact was trained with) ---
scikit-learn==1.5.2
xgboost==2.1.2
lightgbm==4.5.0
pyod==2.0.2
numpy==1.26.4
pandas==2.2.3
joblib==1.4.2

# --- LLM triage ---
anthropic==0.39.0

# --- Persistence ---
sqlalchemy==2.0.36

# --- Outbound HTTP (webhook dispatch) ---
httpx==0.27.2
tenacity==9.0.0

# --- Observability ---
prometheus-fastapi-instrumentator==7.0.0
langfuse==2.53.6
structlog==24.4.0

# --- Configuration ---
pyyaml==6.0.2
